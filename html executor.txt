javascript:(function(){document.body.innerHTML+='<div class="ship" id="ship" style="z-index:1001;position:absolute;"><img id="ship_image" style="z-index:1001;user-select:none;width:120px;position:relative;" src="https://png.pngtree.com/png-clipart/20220619/original/pngtree-x-wing-png-png-image_8103180.png" alt="space_ship"><p id="barrel" style="background-color:transparent;width:4px;height:4px;position:absolute;left:16px;bottom:70px;"></p><p id="barrel2" style="background-color:transparent;width:4px;height:4px;position:absolute;left:100px;bottom:70px;"></p><div id="fire" style="z-index:1000;left:39px;bottom:-8px;border-radius:20px;width:1px;height:1px;border-left:3px solid transparent;border-right:1px solid transparent;border-top:35px solid red;position:absolute;"></div><div id="fire2" style="z-index:1000;left:75px;bottom:-8px;border-radius:20px;width:1px;height:1px;border-left:3px solid transparent;border-right:1px solid transparent;border-top:35px solid red;position:absolute;"></div></div>';var elementsToDestroy=document.querySelectorAll("div");var ship=document.getElementById("ship");var barrel=document.getElementById("barrel");var barrel1=true;var barrel2=document.getElementById("barrel2");document.getElementById('ship_image').setAttribute('draggable',false);var barrelRect;var rect;rect=ship.getBoundingClientRect();ship.style.top=window.innerHeight/2-ship.offsetHeight/2+'px';ship.style.left=window.innerWidth/2-ship.offsetWidth/2+'px';let shipX=0;let shipY=0;let shipRotation=0;let start_speed=0.5;let speed=0.5;const maxSpeed=8;const rotationSpeed=2;const bulletSpeed=15;var forward=false;var backward=false;var left=false;var right=false;var bullets=[];document.addEventListener('mousedown',function(event){if(barrel1){barrelRect=barrel.getBoundingClientRect();}else{barrelRect=barrel2.getBoundingClientRect();}var bullet=document.createElement('div');bullet.style.width="4px";bullet.style.height="24px";bullet.style.position="absolute";bullet.style.backgroundColor="cyan";bullet.style.left=barrelRect.left+window.scrollX+"px";bullet.style.top=barrelRect.top+window.scrollY+"px";bullet.style.zIndex="1000";document.body.appendChild(bullet);bullets.push({element:bullet,x:0,y:0,rotation:shipRotation});barrel1=!barrel1;setTimeout(()=>{if(bullet&&bullet.parentElement){bullet.parentElement.removeChild(bullet);var index=bullets.indexOf(bullet);if(index!==-1){bullets.splice(index,1);}}},1500);});document.addEventListener('keydown',function(event){if(event.code==="KeyW"){forward=true;}if(event.code==="KeyS"){backward=true;}if(event.code==="KeyA"){left=true;}if(event.code==="KeyD"){right=true;}});document.addEventListener('keyup',function(event){if(event.code==="KeyW"){forward=false;speed=start_speed;}if(event.code==="KeyS"){backward=false;speed=start_speed;}if(event.code==="KeyA"){left=false;}if(event.code==="KeyD"){right=false;}});function checkCollision(bullet,element){var bulletRect=bullet.getBoundingClientRect();var elementRect=element.getBoundingClientRect();return(bulletRect.left<elementRect.right&&bulletRect.right>elementRect.left&&bulletRect.top<elementRect.bottom&&bulletRect.bottom>elementRect.top);}function Update(){if(speed<maxSpeed){speed+=0.05;}if(forward){shipX+=Math.sin((shipRotation*Math.PI)/180)*speed;shipY-=Math.cos((shipRotation*Math.PI)/180)*speed;}if(backward){shipX-=Math.sin((shipRotation*Math.PI)/180)*speed;shipY+=Math.cos((shipRotation*Math.PI)/180)*speed;}if(left){shipRotation-=rotationSpeed;}if(right){shipRotation+=rotationSpeed;}ship.style.transform=`translate(${shipX}px,${shipY}px) rotate(${shipRotation}deg)`;for(let i=0;i<bullets.length;i++){bullets[i].x+=Math.sin((bullets[i].rotation*Math.PI)/180)*bulletSpeed;bullets[i].y-=Math.cos((bullets[i].rotation*Math.PI)/180)*bulletSpeed;bullets[i].element.style.transform=`translate(${bullets[i].x}px,${bullets[i].y}px) rotate(${bullets[i].rotation}deg)`;if(elementsToDestroy.length>0){for(let j=0;j<elementsToDestroy.length;j++){if(bullets[i]&&elementsToDestroy[j]&&checkCollision(bullets[i].element,elementsToDestroy[j])&&elementsToDestroy[j].className!="ship"){elementsToDestroy[j].remove();bullets[i].element.remove();bullets.splice(i,1);i--;break;}}}}window.requestAnimationFrame(Update);}const fireElement=document.getElementById('fire');const fireElement2=document.getElementById('fire2');let isShaking=false;function startShaking(){if(!isShaking){isShaking=true;shakeElement(fireElement);shakeElement(fireElement2);}}function stopShaking(){isShaking=false;}function shakeElement(element){const originalLeft=parseInt(element.style.left);const originalTop=parseInt(element.style.top);const shakeInterval=setInterval(()=>{const newX=originalLeft+getRandomInt(-0.5,0.5);const newY=originalTop+getRandomInt(-0.5,0.5);element.style.left=newX+'px';element.style.top=newY+'px';if(!isShaking){clearInterval(shakeInterval);element.style.left=originalLeft+'px';element.style.top=originalTop+'px';}},1);}function getRandomInt(min,max){return Math.floor(Math.random()*(max-min+1))+min;}startShaking();window.requestAnimationFrame(Update);document.addEventListener('keydown',function(event){if(event.code=='KeyE'){let element1=prompt("Enter HTML element to destroy, e.g. div(blocks), a(links), p, h1, h2, span(text elements) and etc.","");elementsToDestroy=document.querySelectorAll(element1);}});let element1=prompt("Enter HTML element to destroy, e.g. div(blocks), a(links), p, h1, h2, span(text elements) and etc.","");elementsToDestroy=document.querySelectorAll(element1);})();